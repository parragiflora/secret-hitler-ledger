{
  "name": "interhuman-secret-hitler",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=@interhuman/shared && npm run test --workspace=@interhuman/server",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=@interhuman/server",
    "dev:client": "npm run dev --workspace=@interhuman/client",
    "build": "npm run build --workspace=@interhuman/shared && npm run build --workspace=@interhuman/server && npm run build --workspace=@interhuman/client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^5.6.3"
  }
}
